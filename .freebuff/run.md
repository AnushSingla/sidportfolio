# Run Doc — Siddhartha Portfolio

## Reproduce uncommitted artifacts
No special env files needed. The workspace and main checkout are the same location.
`node_modules` is already present. If missing, run `npm install`.

## How to run the dev server
```
npm run dev
```
This starts Vite on the default port **5173** with `--host` for network access.
